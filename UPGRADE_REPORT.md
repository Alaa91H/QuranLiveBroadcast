# تقرير ترقية Quran 24/7

تاريخ المراجعة: 2026-09-10

## نطاق الترقية

تمت مراجعة طبقة البيانات، API، التخزين المؤقت، واجهة البطاقات، التوقيت المحلي، مواقيت الصلاة، عرض القرآن والتفسير، ثم إضافة اختبارات QA قابلة للتشغيل.

## ما تغير فعلياً

### العواصم

- قائمة محلية ثابتة من 195 دولة/عاصمة، بدل الاعتماد على استعلام خارجي للعاصمة عند كل دورة.
- 11 بطاقة في الدورة الواحدة: بطاقة رئيسية + 10 بطاقات إضافية.
- 18 صفحة تدوير تغطي 195 عاصمة بالكامل، والصفحة الأخيرة تحتوي 8 بطاقات.
- تم تصحيح السجلات ذات الإحداثيات الصفرية أو أسماء العواصم الخاطئة.
- تمت إضافة `nameAr`, `capitalAr`, `lat`, `lon`, `timezone`, `flag`, وبيانات ملاحظة عند الحاجة.
- العلم يُبنى من ISO alpha-2 كرمز علم فعلي، بينما اسم المدينة والدولة في المركز البصري للبطاقة.

### الطقس

- Open-Meteo هو مصدر الطقس الحالي.
- التخزين المؤقت لكل عاصمة لمدة 24 ساعة.
- مفتاح التخزين المؤقت يتضمن رمز الدولة والإحداثيات لضمان عزل المدن.
- لا يوجد polling شبكي للطقس من المتصفح.

### مواقيت الصلاة

- المصدر الأساسي: eSalah بحسب إعداد الدولة مع الإحداثيات والتاريخ والمنطقة الزمنية والمذهب.
- المصدر الثانوي: AlAdhan مع طرق حساب محلية مختارة عند توفر Convention واضح.
- لا يتم دمج الأوقات المختلفة بقيمة اصطناعية؛ تتم المقارنة وإظهار حالة `verified` أو `review` أو `divergent`.
- التخزين المؤقت لكل مدينة/تاريخ/طريقة لمدة 24 ساعة.
- تجميع الطلبات المتزامنة لنفس المفتاح عبر `inflight` لمنع الازدواج غير الضروري.

### الوقت الحالي

- الساعة المعروضة للمستخدم محسوبة داخل المتصفح بواسطة `Intl.DateTimeFormat` وIANA timezone.
- لا يوجد طلب شبكة كل ثانية؛ المؤقت محلي للعرض فقط.

### القرآن والتفسير

- المحتوى يُجلب من الخادم ويُخزّن 24 ساعة.
- يدعم Al Quran Cloud للنص، QuranEnc للترجمة الإنجليزية، وQuran Foundation للتفسير عند توفير الاعتمادات.
- وضع QA `/?qa=1` يستخدم البقرة 2:282 لاختبار أطول آية، مع تحميل نص تفسير طويل لاختبار التوافق والتفاف السطور.
- التفسير الطويل لا يسبب ضغطاً على البطاقة؛ له مساحة داخلية قابلة للتمرير عند الحاجة.

### الواجهة

- تصميم 16:9 مخصص للبث مع تدرج بصري أوضح، تباين أعلى، خطوط أكبر، ومساحات متوازنة.
- أسماء المدينة والدولة في منتصف البطاقة، والعلم منفصل بصرياً.
- `ResizeObserver` وقياسات overflow لتقليل حالات القص على العبارات الطويلة.
- دعم `container-type` في التخطيط لضبط البطاقة بحسب عرض الحاوية.

## الاختبارات المنفذة

- فحص syntax لـ `web/server.js`, `web/app.js`, `scripts/qa.js` عبر `node --check`.
- تحقق من 195 سجلًا، عدم وجود أكواد مكررة، عدم وجود إحداثيات صفرية، وعدم وجود حقول أساسية ناقصة.
- تحقق HTTP لجميع الصفحات الـ18 في وضع QA المحلي.
- تحقق من أن كل صفحة تحتوي العدد المتوقع من المدن، وأن الصفحة الأخيرة تحتوي 8 مدن.
- تحقق من endpoint البقرة 2:282.
- تحقق ثابت من وجود cache لمدة 24 ساعة، وفصل مفاتيح الطقس والصلاة، وعدم وجود polling شبكي للطقس/الصلاة من المتصفح.

## ملاحظات الدقة

مواقيت الصلاة ليست رقماً عالمياً واحداً؛ اختلاف method أو madhab أو convention قد يسبب فروقاً مشروعة. لذلك تعرض الواجهة حالة المقارنة بدلاً من ادعاء تصحيح مطلق.

## مصادر البحث الرئيسية

1. REST Countries: https://restcountries.com/
2. eSalah Developer API: https://www.esalah.com/developer.html
3. AlAdhan Prayer Times API: https://aladhan.com/prayer-times-api
4. PrayTimes: https://praytimes.org/
5. Open-Meteo API: https://open-meteo.com/en/docs
6. Quran Foundation API: https://api-docs.quran.com/
7. QuranEnc API: https://quranenc.com/en/home/api
8. Government of Equatorial Guinea: https://www.guineaecuatorialpress.com/
9. South African Government: https://www.gov.za/
10. Sri Lanka Government: https://www.gov.lk/
11. Kazakhstan Akorda: https://www.akorda.kz/
